/**
 * Schema version for chat history compatibility
 * Increment this when chat message format changes
 */
export const SCHEMA_VERSION = "0.0.1";

/**
 * Current app version (should match package.json version)
 */
export const APP_VERSION = "0.0.1";
